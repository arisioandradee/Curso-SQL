-- DAY: retorna o DIA da data utilizada na função
SELECT DAY(last_update)
FROM actor;

-- MONTH: retorna o MÊS da data utilizada na função
SELECT MONTH(last_update)
FROM actor;

-- YEAR: retorna o MÊS da data utilizada na função
SELECT YEAR(last_update)
FROM actor;