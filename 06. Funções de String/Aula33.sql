-- REPLACE: troca alguma parte da string por outra que foi enviada no parametro
	-- REPLACE (texto, antigo, novo)
    
SELECT name FROM category

SELECT REPLACE(name, 'Action', 'Comedy') as alteracoes FROM category